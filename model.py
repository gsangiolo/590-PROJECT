class ModelParams():
    def __init__(self, epochs, layers, regularization):
        self.epochs = epochs
        self.layers = layers
        self.regularization = regularization

    def train_model(self):
        # Do something in the background/async
        return 
